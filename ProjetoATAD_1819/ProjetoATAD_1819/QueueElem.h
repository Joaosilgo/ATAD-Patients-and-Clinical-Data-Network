#pragma once
#pragma once
#include "Patient.h"
/* Definição do tipo de dados (exemplo para Patients) */
typedef Patient QueueElem;

/* Definição de quaisquer outras operações que o tipo deve
suportar, e.g., imprimir, comparar, etc. */
void queueElemPrint(QueueElem elem);
